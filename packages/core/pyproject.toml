[project]
name = "alpha-core"
version = "0.1.0"
description = "Core components and utilities for the Alpha ecosystem."
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["litellm>=1.86.0", "pydantic>=2.13.4", "python-dotenv>=1.2.2"]
