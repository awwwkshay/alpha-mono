# clay-studio-server

Local FastAPI backend for `clay studio`.

The server reads a Clay project from the current working directory, exposes
Studio APIs for project state, and serves the embedded React/Vite build.
