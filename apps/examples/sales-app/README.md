# sales-app

A Clay app generated with `clay init`.

## Usage

```bash
cp .env.example .env
uv run sales-app
```

Open Clay Studio:

```bash
uv run clay studio
```

## Workspaces

The app includes a local workspace at `workspaces/sales-workspace/`. Agents can
use it for sales files, generated artifacts, and sandboxed command execution.
